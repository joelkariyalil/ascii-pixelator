# ascii-pixelator

Takes an image, and converts it into a binary image consisting of 0s and 1s.

Make this a little more interesting with
- additional symbols, than 0s and 1.
- create project in Rust
